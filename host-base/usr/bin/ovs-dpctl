#! /usr/bin/env sh
dir="$(dirname "$0")"
export RUNAS_ARG0="$0"
export LD_PRELOAD="$dir//bundles/ovs-dpctl/lib/runas.so"
exec "$dir//bundles/ovs-dpctl/lib/ld-linux-x86-64.so.2" --library-path "$dir//bundles/ovs-dpctl/lib/" "$dir/.ovs-dpctl.bin" "$@"
